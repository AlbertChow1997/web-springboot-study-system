<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace: 命名空间 -->
<mapper namespace="com.albert.mapper.DeptMapper">

    <!--  在mapper标签中，去写对应的SQL语句-->
    <!--    select  查询语句 -->
    <!--    update  修改语句 -->
    <!--    insert  增加语句 -->
    <!--    delete  删除语句 -->

    <!--  id: 标签的id值   resultType：sql语句查询结果的返回值类型 -->
    <select id="findAll" resultType="com.albert.pojo.Dept">
        select id, dept_name as name, create_time, update_time from department order by update_time desc
    </select>

    <delete id="deleteById">
        delete from department where id = #{id}
    </delete>
    <insert id="add">
        insert into department (dept_name, create_time, update_time)
        values (#{name}, now(), now())
    </insert>

    <select id="getInfo" resultType="com.albert.pojo.Dept">
        select id, dept_name as name, create_time, update_time from department where id = #{deptId}
    </select>

    <update id="update">
        update department set dept_name = #{name}, update_time = now() where id = #{id}
    </update>
</mapper>